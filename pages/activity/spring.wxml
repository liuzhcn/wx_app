<!--pages/activity/spring.wxml-->
<view class="contain">
  <image mode="widthFix" class="qidongye" src="/images/activity-2.jpg"></image>
  <!-- <button class="yaoyiyao_btn" bindtap="yaoyiyao">开始摇一摇</button> -->
  <!-- 遮罩层 -->
  <!-- <view class="zhezhaoceng" wx:if="{{isShow}}">
    <image class="zhezhaoimg" src="/images/14.png"></image>
  </view> -->
  <view class="page__bd jiugongge">
    <view class="weui-grids">
      <block wx:for="{{grids}}" wx:key="*this">
        <view class="weui-grid" hover-class="weui-grid_active" bindtap='openApp' data-url='{{item.url}}' data-type='{{item.type}}' data-auth='{{item.auth}}'>
          <image class="weui-grid__icon" src="{{item.icon}}" />
          <!-- <view class="weui-grid__label">{{item.name}}</view> -->
        </view>
      </block>
    </view>
  </view>
</view>